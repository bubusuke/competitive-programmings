#include <algorithm>
#include <vector>
using namespace std;

int main() {
  vector<int> v(n);

  //順列生成
  do{
    //vは次の順列になる
    for(int i=0;i<n;i++){
          //なんらかの操作
    }
  }while(next_permutation(v.begin(),v.end()));
}